# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

BusTowns is a web project in its earliest stage. Currently the repo contains only a bare `index.html`.

## Development

No build tooling, package manager, or test framework has been set up yet. To view the site, open `index.html` directly in a browser or serve it with any static file server:

```
npx serve .
# or
python3 -m http.server
```

As the project grows, update this file with build commands, test commands, and architecture notes.
